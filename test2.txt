programa pruebaPrograma ;

variables:
    a, b : entero;

void funcion pelos (x: entero, y: entero, z: entero)
{
    a = 3 * z;
    escribe(a);
    regresa(a);
}


principal () {
    b = 5;
    a = b * 4;
    escribe(a);
    pelos(88, a, a+b*2*10);
    escribe(b);
    si (b > 0) entonces {
        escribe("Meno 3 rSE r");
    } sino {
        escribe("mayor 3 SDF ");
    }
    escribe(a);
}