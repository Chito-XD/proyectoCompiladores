programa pruebaPrograma ;

Clase Cabeza {
    atributos:
        f: flotante;
        e: entero;
    
    metodos: 
    void funcion uno () {
        f = 1.3;
    }

    entero funcion dos() {
        escribe(1*2);
    }
}


Clase Brazo {
    atributos:
        f: flotante;

    metodos:
    entero funcion dos() {
        escribe(1*2);
    }
}


variables:
    k[2], i, j, p, c, a, b, d, e, f: entero; 
    r: flotante;
    
entero funcion fact (r : entero) 
variables:
    i : entero;
{ 
    c = 1 + 1;
    i = j + ( p - j ) * (d + 4 / f * i) / 2 + p;
    si (j ==1) entonces {
        regresa(j);
    } sino {
        regresa(j*3);
    }
    j = j + 2;
}

void funcion pelos (y : entero, n : entero);
variables:
    x, i: entero;
{
    x = y;
    mientras (x<10) hacer {
        x = x + 1;
    }

    escribe(n);

    x = 1;

    desde i = 0 hasta x * 5 hacer {
        y = 2 * i;
    }
}

principal () {
    pelos(1, 2);
    escribe(1*f+j);
    fact(i);
    lee(p); 
    lee(c[0]);
    escribe(a.b);
    i = 2;
    r = 1.0;
    lee(k[2][2]);
    j = p * 2;
    i = 3;
    pelos(1,2);
    escribe("Hola");
}