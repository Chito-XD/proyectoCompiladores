programa test1 ;

variables i, j, p: entero; estudiante : entero ;

entero funcion fact (j : entero); 
variables i : entero;
{ 
    i = j + (p -j*2+j);
    si (j==1) entonces {
        regresa(j);
    } sino {
        regresa(j*fact());
    }
}

void funcion pelos (y : entero);
variables x: entero;
{
    leer(estudiante);
    x = y;
    mientras (x<10) hacer {
        escribir(estudiante);
        x = x + 1;
    }
}

principal () {
    leer(p); j = p * 2;
    i = fact(p);
    leer(estudiante);
    pelos(p);
    desde i = 1 hasta 10 hacer {
        escribir("hola", alumno, fact(edad), "hol");
    }
}