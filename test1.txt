programa pruebaPrograma ;

variables k[2], i, j, p: entero; estudiante : Persona ;

entero funcion fact (j : entero); 
variables i : entero;
{ 
    i = j + p - j * 2 + j;
    si (j==1) entonces {
        regresa(j);
    } sino {
        regresa(j*3);
    }
}

void funcion pelos (y : entero);
variables x: entero;
{
    x = y;
    mientras (x<10) hacer {
        x = x + 1;
    }
}

principal () {
    leer(p); j = p * 2;
    i = 3;
    pelos(p);
    escribe("Hola");
}