programa pruebaPrograma ;

Clase Cabeza {
    atributos:
        f: flotante;
        e: entero;
    
    metodos: 
    void funcion uno () {
        f = 1.3;
    }

    entero funcion dos() {
        escribe(1*2);
    }
}


Clase Brazo {
    atributos:
        f: flotante;

    metodos:
    entero funcion dos() {
        escribe(1*2);
    }
}


variables:
    k[2], i, j, p, c, a, b, d, e[5][3], r: entero; 
    f: flotante;
    
entero funcion fact (r : entero) 
variables:
    i : entero;
{ 
    si (r == 1) entonces {
        regresa(r);
    } sino {
        regresa(r * fact(r-1));
    }
}

void funcion pelos (y : entero, n : entero)
variables:
    x, i: entero;
{
    x = y;
    mientras (x<10) hacer {
        x = x + 1;
    }

    escribe(n);

    x = 1;

    desde i = 0 hasta  x * 5 +2  hacer {
        y = 2 * i;
    }
}

entero funcion fibo (n: entero)
{
    si (n <= 1) entonces {
        regresa(n);
    } sino {
        regresa(fibo(n-1) + fibo(n-2));
    }
}

principal () {
    escribe( fibo( 10 ) );
    e[2][3] = 2;
    a = e[2][3];
    f= 0.4;
    
    escribe("FINAL", a);
}
