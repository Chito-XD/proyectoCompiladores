programa pruebaPrograma ;

variables k, i, j, p, c: entero; estudiante : Persona ;

entero funcion fact (j : entero); 
variables i : entero;
{ 
    c = 1 + 1;
    i = j + p - j * 2 + j;
    si (j==1) entonces {
        regresa(j);
    } sino {
        regresa(j*3);
    }
}

void funcion pelos (y : entero);
variables x: entero;
{
    x = y;
    mientras (x<10) hacer {
        x = x + 1;
    }
}

principal () {
    lee(p); j = p * 2;
    i = 3;
    pelos(p);
    escribe("Hola");
}